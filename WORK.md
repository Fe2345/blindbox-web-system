负责写：
6.2 第一部分：基础主体与商品供给模块设计
6.2.1 用户管理程序设计说明
6.2.2 商家管理程序设计说明
6.2.3 商品信息管理程序设计说明
6.2.4 供货管理程序设计说明
重点写清楚：
用户注册登录、个人信息、地址管理、角色区分；商家入驻申请、审核通过、审核驳回、禁用；商品提交、商品审核、上下架、库存状态；供货商品与奖池、订单发货之间的关系。
可以按这个格式写：
6.2.1 用户管理程序设计说明

1. 程序描述
用户管理程序主要负责普通用户、商家用户和管理员的登录认证、注册信息维护、角色识别和地址管理。该程序是系统其他业务功能的基础，用户只有完成身份认证后，才能继续执行盲抽、资产管理、资源转化、换物、订单处理、商家供货和后台审核等操作。

2. 功能
本程序主要包括用户注册、用户登录、用户登出、个人信息查询与修改、收货地址新增、收货地址修改、收货地址删除、默认地址设置和用户角色识别等功能。系统应校验用户名唯一性、密码长度、手机号或邮箱格式，并对用户密码进行加密存储。

3. 输入项
用户注册输入项包括用户名、密码、手机号或邮箱、验证码等信息。
用户登录输入项包括用户名、密码和验证码。
个人信息修改输入项包括昵称、联系方式、头像等信息。
地址管理输入项包括收货人姓名、手机号、详细地址和默认地址标识。

4. 输出项
注册成功时输出用户编号、注册状态和提示信息。
登录成功时输出登录状态、用户编号、用户角色和会话信息。
操作失败时输出错误状态码和错误提示，例如用户名已存在、密码格式错误、验证码错误、用户未登录等。

5. 处理逻辑
用户提交注册信息后，系统先进行字段合法性校验，再查询用户名或手机号是否已经存在。校验通过后，系统对密码进行哈希处理，并写入用户表。用户登录时，系统根据用户名查询用户记录，验证密码是否正确，若连续 5 次登录失败，则临时锁定账户 15 分钟。用户访问受保护页面时，系统调用用户认证接口和权限校验接口，判断用户是否已登录以及是否具备对应操作权限。

6. 接口
本程序调用用户认证接口、权限校验接口、统一响应接口、异常处理接口和操作日志接口。
主要业务接口包括用户注册接口、用户登录接口、用户登出接口、个人信息获取接口、个人信息修改接口、地址列表接口、地址新增接口、地址修改接口和地址删除接口。

7. 数据结构
本程序主要访问用户表、用户角色表、用户地址表和登录会话数据。用户表保存用户编号、用户名、密码摘要、联系方式、用户角色、账号状态和创建时间。地址表保存地址编号、用户编号、收货人、联系电话、详细地址和默认地址标识。

8. 限制条件
系统不保存明文密码。未登录用户不能访问个人中心、资产管理、换物、订单和后台管理等受保护功能。普通用户不能访问商家后台和管理员后台。被禁用用户不能继续执行登录后的业务操作。

9. 测试计划
测试注册字段合法性、用户名重复、密码错误、连续登录失败锁定、登录成功、个人信息修改、地址新增修改删除、默认地址设置和未登录访问拦截等场景。
其他三个模块就照这个格式写。
